   var video = document.createElement('video');
    video.autoplay = true;
    video.playsinline = true;
    video.width = 300;
    video.height = 300;
    video.className = "peersFace";
    var div = document.getElementById('myStream');
    div.appendChild(video);
